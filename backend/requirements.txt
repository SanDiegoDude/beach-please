# Beach, Please backend — pip requirements.
# Pinned to floors that match pyproject.toml. Use `uv` if you have it,
# otherwise: python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
openai>=1.54.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0
sse-starlette>=2.1.0
